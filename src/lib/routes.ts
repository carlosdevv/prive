export enum BASE_ROUTES {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  DASHBOARD = '/dashboard',
  SETTINGS = '/settings'
}

export enum DASHBOARD_ROUTES {
  HOME = '/',
  ASSETS = '/assets'
}
